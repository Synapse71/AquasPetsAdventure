# 桌宠 Demo 界面设计基线

> 2026-09-09：桌面客户端进入 UE 实施设计。最新已确认行为见 [桌宠客户端 UE 实施共识](docs/desktop-ue-decisions.md)，与本文旧条目冲突时以该文为准。以下已同步主要变更。

## 产品形态

这不是一个以 Dashboard 为首页的网页游戏。首屏必须先被识别为“一个活在桌面上的宠物”，系统功能是围绕宠物展开的第二层界面。

界面层级固定为：

1. 桌面舞台：宠物本体、动作、气泡、极少量资源状态。
2. 宠物快捷菜单：点击宠物后出现，提供探险、库存、宠物、图鉴、设置五个入口；任务归入探险系统。
3. 功能浮窗：一次只打开一个子系统，保留定稿原型的大面板尺寸，优先出现在宠物左侧或右侧。空间不足时允许暂时遮挡宠物，保持宠物原位置。
4. 系统设置：从宠物快捷菜单进入，不在桌面舞台上额外常驻入口。

## 三秒原则

玩家进入后的前三秒，只需要理解三件事：

- 这是我的宠物。
- 它现在在做什么、是否需要我处理。
- 点击它可以互动或打开功能。

首屏不得同时铺开探险、库存、任务、属性、图鉴等完整信息。

## 状态表达

宠物动作本身是主要状态反馈：

- 空闲：观察、待机，气泡邀请玩家点击。
- 行进：行走动画，气泡显示目的地和剩余时间。
- 等待决策：静止灯泡提醒玩家处理事件、路线或撤离；待办不影响睡眠，也不自动唤醒宠物。
- 满载而归：挖掘动画，气泡提示本轮战报可看。战利品在确认撤离时已直接入库，这里没有待办操作。

功能入口上的数字徽标只承担次级提醒，不替代宠物反馈。

## 视觉语言

- 舞台：背景透明，让宠物成为最大视觉物体，并为 Electron 透明窗口预留实现空间。
- 色彩：灰绿与暖米色作为环境色，黄色只来自宠物和必要提醒。
- 浮窗：温和的浅色实体面板，保留定稿原型的尺寸（状态宽 500px，库存宽 812px，行动/图鉴/设置宽 760px），按需打开。内部交互不改变面板外框尺寸。
- 圆角：舞台提示与菜单较圆；数据卡与表单采用中等圆角。
- 字体：系统无衬线负责信息，Georgia 只用于名称和窗口标题。
- 动效：宠物资源承担主要动效；界面只使用短促的淡入与位移。

## 交互约束

- 点击宠物：展开或收起快捷菜单。
- 点击气泡：直接进入当前状态最相关的功能浮窗。
- 点击浮窗外部或关闭按钮：回到桌宠舞台。
- 点击桌面或其他应用同样收起浮窗；关闭保留所在页面和未确认操作，不自动提交。
- 隐藏后重新显示、退出后重新启动只显示宠物，主动打开面板时再恢复工作进度。
- 同一时间只允许一个功能浮窗。
- 探险、库存等信息较多的系统使用页签切换，避免同时展开全部内容。
- 功能窗属于非模态桌面悬浮组件，不使用全屏遮罩。
- 远征事件、路线选择和撤离仍合并在“探险”浮窗的连续流程里。
- 移动端将侧边浮窗降级为底部抽屉，但保留同样的层级。

## 暂不决定

- 世界观、具体桌面房间或赛博空间设定。
- 最终宠物名称、功能图标和正式美术背景。
- 多宠物同时出现在桌面上的空间编排。

这些内容不得反向限制当前玩法和界面架构。
