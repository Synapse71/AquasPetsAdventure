# 桌宠与气泡尺寸（2026-09-14）

- 大小滑杆范围 130–420px，步长 10px，默认仍为 300px。130px 是旧最小档 260px 的一半，指宽高各减半，不是面积减半。
- 数值指动画画布宽度，包含素材透明留白，不等于角色可见轮廓宽度。
- 气泡最小档调整为 16px，间距 3px，整行 92px（原最小菜单 184px 的一半）；130–300px 之间插值增长到直径 42px、间距 6px，300px 以上保持 `round(canvas * 0.14)`，最大档 59px。每个按钮使用原生 `title` tooltip 显示功能名称，并保留 `aria-label`。
- 小尺寸下菜单可以宽于本体，几何布局为菜单左右溢出及底部完整高度预留空间，边缘拖拽不裁切菜单。
- 设置预览与桌面气泡共享 `desktop/geometry.mjs` 的 `petMetrics`，主进程保存/恢复与滑杆共享上下限。
- 现有尺寸偏好保持原数值；不会强制缩小所有玩家的宠物。玩家可自行选择新增的小尺寸档位。
- 大面板尺寸不变，原有动画和素材不变。行进信息保留至少 140px 宽度以避免最小档文字过度拥挤。
- 滑杆左键按下时通过 settings IPC 开始缩放事务，锁定当前原生窗口 bounds 和面板位置；拖动仅实时预览本体与气泡尺寸。预览时本体必要时限制在冻结窗口内。松开、取消指针、失去指针捕获或窗口失焦时结束事务并重新定位；关闭/切换面板也释放锁。键盘调整无按住鼠标事务，即时正常定位。

验证：布局单测包含 130–420px、屏幕边缘、负坐标屏幕及打开面板位置稳定性、缩放预览锁定；Electron 设置烟测检查真实画布大小、16px 气泡预览一致性、保存/重载、五个气泡完整可见与 tooltip，以及按下—连续改值—释放/取消的窗口/面板/滑杆坐标。

烟测另使用 Chromium 原生鼠标输入拖动滑杆滑块，确认每次移动确实改变尺寸且窗口、面板和滑杆均固定，松开后恢复定位，避免只验证合成 DOM 事件。

开发注意：Vite 热更新只更新页面，不会重载 Electron 主进程。修改 `desktop/main.cjs` 或其几何模块后，需要从设置中「退出游戏」并重新启动桌面进程；关闭面板、隐藏宠物或刷新页面均不等于重启。2026-09-14 的录屏反馈确认属于旧主进程（10:41 启动）仍在运行，而位置锁定代码于 10:56 更新。
