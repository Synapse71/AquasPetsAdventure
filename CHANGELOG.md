# 更新日志

版本号对应 `release/` 下的安装包文件名。每个版本都打了同名 git tag。

---

## 0.1.1 · 2026-09-18

`0.1.0`（`ea35c90`）以来的 17 个提交。

### 玩法

**食物可以吃了。** 此前食物只是能卖钱的战利品，现在可以吃掉换一次性效果：提升一项主属性（整趟冒险有效），或按档恢复伤势。两种效果互相独立，一件食物可以只有其一、也可以都有；哪些食物配、配多少由配表决定，规则层不看稀有度。

- 同时只存在一个加成，后吃的顶掉前面的
- 三个入口：行前整备从仓库选定（出发那一刻才扣）、途中在节点上从背包吃、基地用仓库里的食物治疗
- 加成同时抬高负重和格子上限，只加一次，不随队伍人数放大
- **食物不影响次要属性**——次要属性是硬门槛，食物能抬高门槛就等于食物是钥匙，那它会变回「为了过那扇门必须刷食物」的必需品

这条重开了设计文档 §9/§12 曾经否决的方向，所以文档里记了为什么：被否决的是**必需品循环**（购买、补货、为维持正常冒险反复刷），而食物只从掉落获得、永不成为通关前提、而且和它自己的售价相互竞争，是战利品的**出口**而不是出发前的**清单**。完整论证和六条不变量见 [系统设计 §9.3](docs/game-system-design.md)。

**开箱动画按稀有度分档。** 六个稀有度各有一段专属开箱视频，闭合画面、播放和播完的定格全部来自同一套画布，所以宝箱不会中途换外观或跳尺寸。没有素材的档位自动回退到原来的木箱动画。

**地图编辑改成图上直接操作**（0.1.0 之后、本次打包之前的工作）：点节点编辑节点，点连线编辑路线，节点 XP 可按地图配置，并落地了一轮地图时长、掉落与经验的平衡调整。

### 界面

- 探险面板去掉深色模式，六个面板统一白底
- 派遣界面的宠物卡片去掉了头像上下的灰色空缺，卡片高度改为由内容决定
- 探险中的工具栏会显示当前生效的食物加成；风险说明会注明食物的贡献，但只在加成正好作用于本次检定的属性时才提
- 固定面板书签、桌宠面板尺寸统一、抵达动画与出征准备默认值修整（0.1.0 之后的既有工作）
- 宠物头像接入，统一用于状态、派遣与图鉴

### 配置台（仅开发模式）

- 物品表单新增「食物效果」编辑区；主属性下拉里只有体能／感知／技巧，配合目录校验从两层堵死「食物加成次要属性」
- 配了食物效果却没打「食物」标签会出提醒
- 配置台与本地目录覆盖只在开发模式加载，生产构建完全摇掉

### 开发工具（不进安装包）

- 新增 GM 后台：`npm run dev:desktop` 下窗口左下角，可立即抵达、快进倒计时、治疗基地宠物、调整金币。两道闸保证它进不了安装包——生产构建断言模块图里没有 `src/gm/`，打包校验再扫一遍真实归档
- `tools/build-chest-rarity.py`：把开箱原片转成成品素材，并在各档构图偏差超过阈值时中断
- macOS / Windows 开机自启动开关

### 修复

- 开箱动画偶发闪白：原来点击才创建视频元素、播完才创建定格图，每次切换都有一帧画不出东西。现在三层全程挂载，播放改为命令式
- 非堆叠物品不再显示多余的数量角标
- 抵达动画的白边残留清理

### 验证

`npm run build`、299 条单元测试、开箱真机冒烟 30 条断言、库存面板冒烟 11 条断言全部通过。

**已知问题**：`tools/adventure-prototype-smoke.mjs` 里 `unsellable cargo cannot be implicitly sold` 一条失败，`0.1.0` 之前就存在。原因是该用例把改过的目录写进 localStorage 的「已发布目录」，而冒烟跑的是生产构建，生产构建按设计不读取它。要么让这段指向开发服务器，要么在内置目录里放一件本来就不可出售的物品（目前一件都没有）。

---

## 0.1.0 · 2026-09-10

首个可玩 Demo 包（`ea35c90`）。桌宠、地图探险、事件判定、负重与撤离、仓库与图鉴、策划配置台。

当时忘了打 tag，`0.1.1` 发布时依据 `release/` 下产物的时间戳回补。
